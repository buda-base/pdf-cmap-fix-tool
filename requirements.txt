# Web framework
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# PDF processing
pymupdf>=1.24
pymupdf4llm>=0.0.17

# Word (.docx) export for extracted text. Used as the fallback renderer and for
# the plain-text (legacy-Tibetan) path; pandoc, if installed, is preferred for
# Markdown fidelity (headings, tables, lists).
python-docx>=1.1

# CMap repair (OpenPecha) — the core tool this app wraps
pdf-cmap-fix @ git+https://github.com/OpenPecha/pdf-cmap-fix.git

# Legacy Tibetan font -> Unicode (BDRC). Experimental; only convert_string +
# normalize_font_name are used. Pulls in pdfminer.six + fonttools transitively.
pytiblegenc @ git+https://github.com/buda-base/py-tiblegenc.git
